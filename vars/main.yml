---
# Role variables

# Packages to remove before install

obsolete_docker_engine_packages:
  redhat: []

  debian:
    - docker
    - docker-engine
    - docker.io

# Packages to install

docker_engine_packages:
  redhat_6:
    - docker-io
    - python-docker-py
    - libselinux-python

  redhat_7:
    - docker-io
    - python-docker
    - libselinux-python

  debian_stretch:
    - apt-transport-https
    - docker-ce
    - python-docker
