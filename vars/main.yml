---
# Role variables

# Packages to remove before install

obsolete_docker_engine_packages:
  redhat: []

  debian:
    - docker
    - docker-engine
    - docker.io

# Packages to install

docker_engine_packages:
  redhat_6:
    - docker-io
    - libselinux-python
    - python-docker-py
    - python-urllib3

  redhat_7:
    - docker-io
    - libselinux-python
    - python-docker
    - python-urllib3

  debian_stretch:
    - apt-transport-https
    - docker-ce
    - python-docker
    - python-urllib3
