---
# Tasks for testing role

- name: configure vagrant sandbox environment
  hosts: localhost
  roles:
    - role: amtega.vagrant_presets
      vagrant_presets_boxes_json_query: >-
        [? (starts_with(name, `centos_7`))
           || starts_with(name, `debian`)
           || starts_with(name, `fedora`) ]
  tags:
    - sandbox

- name: run idempotence test
  hosts: localhost
  roles:
    - role: amtega.vagrant_sandbox
      vagrant_sandbox_state: started
  tags:
    - sandbox

- name: test vagrant_engine role
  hosts: vagrant_sandbox_vms
  roles:
    - role: amtega.docker_engine
      docker_engine_users:
        - root
  tasks:
    - name: get vagrant info
      shell: vagrant info
      register: vagrant_info_result
      changed_when: false

    - name: verify vagrant info
      assert:
        that: >-
          {{ vagrant_info_result.stdout is search("Server Version")
             or vagrant_info_result.stdout is search("Storage Driver") }}

    - name: check vagrant registry service
      wait_for:
        host: "{{ ansible_facts.host }}"
        port: "{{ vagrant_engine_registry_port }}"
        timeout: 60
        connect_timeout: 5
  tags:
    - idempotence

- name: cleanup vagrant sandbox
  hosts: localhost
  roles:
    - role: amtega.vagrant_sandbox
      vagrant_sandbox_state: absent
  tags:
    - sandbox
